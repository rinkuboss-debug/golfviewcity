{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }  // for Vite, use "build" for CRA
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/" }
  ]
}
